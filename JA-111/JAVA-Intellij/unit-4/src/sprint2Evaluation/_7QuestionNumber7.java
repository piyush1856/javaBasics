package sprint2Evaluation;
//Explain the concept of static in Java.
public class _7QuestionNumber7 {

    /*
    *1. In JAVA static is related to class,variable,instance,method;

    2. we can access static variable inside static method directly;
        but we can not have access to use then inside non-static method directly;

    3. To use them inside non-static method we have to first create an object of thet class,
        after that we can access them through obj_Name.variable_Name;

    4. when we define a variable as static, it gets space on RAM;

    5. non-static variables are inside hardware drive;

    6. it is used for memory management;

    7. its value stored inside is same in program mean is shares single copy of its variable throughout the program;
    * */
}
